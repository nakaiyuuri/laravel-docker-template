# Laravel Lesson レビュー②

## Todo編集機能

### @method('PUT')を記述した行に何が出力されているか
* インプットタグ。
* input type="hidden" name="_method" value="PUT" 

### findメソッドの引数に指定しているIDは何のIDか
* 編集、詳細表示、更新、削除をするレコードのID。

### findメソッドで実行しているSQLは何か
* SELECT * FROM todos WHERE id = 該当のレコードのid;

### findメソッドで取得できる値は何か
* 取得したいレコードに対応するTodoモデルインスタンス。

### saveメソッドは何を基準にINSERTとUPDATEを切り替えているのか
* Todoモデルインスタンスのカラム名のプロパティに既に何らかの値が代入されているかどうかを基準に切り替えている。

## Todo論理削除

### traitとclassの違いとは
* トレイトは一つのクラスに対して複数追加できる。一方でクラスは一つのクラスしか継承できない。
* トレイトはトレイト自身はインスタンス化できないが、クラスはインスタンス化できる。

### traitを使用するメリットとは
* 複数クラスで同じメソッドやプロパティを共有でき、再利用できる。メソッドやプロパティの修正がある場合はトレイト部分を変更するだけなので、保守性が上がる。

## その他

### TodoControllerクラスのコンストラクタはどのタイミングで実行されるか
* TodoControllerクラスがインスタンス化されるタイミング。

### RequestクラスからFormRequestクラスに変更した理由
* バリデーションを行うため。FormRequestクラスはRequestクラスを継承しているので元々使用していたメソッドなどは問題なく動く。

### $errorsのhasメソッドの引数・返り値は何か
* 引数は入力欄のname属性の値、返り値はtrueまたはfalse。

### $errorsのfirstメソッドの引数・返り値は何か
* 引数は入力欄のname属性の値。返り値は該当の入力欄で初めに発生したエラーのエラーメッセージ。

### フレームワークとは何か
* 予め枠組みが用意されており、その枠組みに肉付けするだけで一定品質のプロダクトを作れるようにしたもの。

### MVCはどういったアーキテクチャか
* Model,View,Controllerの頭文字をとっており、開発効率を高めるために作られたアーキテクチャ。

### ORMとは何か、またLaravelが使用しているORMは何か
* ORMとはクラスとテーブルをマッピングし、SQLを直接操作することなく、クラスのメソッドやプロパティを使用してDBとやり取りをするための手法。
* Laravelが採用しているORMはEloquent

### composer.json, composer.lockとは何か
* composer.jsonはインストールするパッケージやライブラリを管理するファイル。
* composer.lockはインストールしたパッケージやライブラリを管理するファイル。インストール時に参照したパッケージやパッケージのバージョン情報が書き込まれる。

### composerでインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか
* ventorディレクトリ。
